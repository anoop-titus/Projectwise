---
phase: 03-browsing-symlinks
type: execute
---

<objective>
Implement folder browser with recursive navigation and info pane. Display metadata for any folder (size, file count, sessions, has PROJECT.json/sessions-index.json).

Purpose: Enable exploration and selection of ALL folders, not just registered projects.
Output: `folder-browse.sh` and `folder-info.sh` scripts with FZF integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Phase goal from ROADMAP:** Folder browsing, symlink organization, rich metadata display.

**Dependency:** Phase 2 (FZF selector with preview capability, folder-info.sh pattern).

**What we're building:**
- Folder browser: Recursively list all directories in ~/.claude/projects/ (max depth 3)
- Rich metadata display: Size, file count, modified date, has PROJECT.json/sessions-index.json
- FZF integration: Search, preview, navigation
- Integration: Accessible from selector via Ctrl-B binding (already exists in Phase 2)
- Return selected folder path for cd or further operations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement folder browser with recursive traversal</name>
  <files>~/.claude/scripts/folder-browse.sh</files>
  <action>
    Create folder-browse.sh script:
    1. Use fd or find to recursively list all directories in ~/.claude/projects/
       - Max depth: 3 (prevents going too deep)
       - Exclude: .git, .cache, node_modules, .tmp, __pycache__
    2. For each folder, collect stats:
       - Folder name and path
       - Is registered in .registry.json? (✓ or □)
    3. Build FZF input with format: "folder_path\tfolder_name\tstatus"
    4. Launch FZF with:
       - Search enabled (type to filter by folder name)
       - Preview pane running folder-info.sh for selected folder
       - Header: "Browse ALL folders in ~/.claude/projects/ (Esc to cancel)"
       - Arrow key navigation (default FZF)
       - Enter to select, Escape to cancel
    5. Extract selected folder path and return it
    6. Handle empty selection gracefully

    This script will be called from project-select.sh via Ctrl-B binding.
  </action>
  <verify>
    Call: ~/.claude/scripts/folder-browse.sh

    Should show FZF interface with:
    - List of all folders (15+ entries)
    - Search/filter working (type to narrow results)
    - Preview pane on right showing folder metadata
    - Selection returns folder path
    - Escape cancels and returns nothing
  </verify>
  <done>Folder browser implemented, shows all folders with recursive traversal, can select any folder</done>
</task>

<task type="auto">
  <name>Task 2: Create folder info script with rich metadata display</name>
  <files>~/.claude/scripts/folder-info.sh</files>
  <action>
    Create folder-info.sh script:
    1. Takes FOLDER_PATH as argument
    2. Collects metadata:
       - Folder name and absolute path
       - Size: du -sh
       - File count: find ... | wc -l
       - Modified date: stat -f %Sm (on macOS)
       - Session count: if sessions-index.json exists, jq '.sessions | length'
    3. Check for metadata files:
       - PROJECT.json exists? (✅ or ❌)
       - sessions-index.json exists? (✅ or ❌)
    4. Check registration status:
       - Is folder in .registry.json? Extract display_name if yes
    5. Render with gum styling:
       - Title: 📁 Folder Name
       - Path: /absolute/path/to/folder
       - Stats: Size, Files, Modified, Sessions
       - Metadata: PROJECT.json status, sessions-index.json status
       - Registration: Display name (if registered) or "Not in registry"
       - Use gum style with border rounded, padding, colors for readability
    6. Handle errors gracefully (folder doesn't exist, permission denied, etc.)

    This script is called as FZF preview for folder-browse.sh.
  </action>
  <verify>
    Call: ~/.claude/scripts/folder-info.sh ~/.claude/projects/claude_1769760221

    Should display:
    - Folder name and path
    - Size, file count, modified date
    - Session count (if available)
    - Metadata file statuses
    - Registration status with display name (if registered)
  </verify>
  <done>Folder info script displays rich metadata, handles missing files gracefully, formatted with gum styling</done>
</task>

<task type="auto">
  <name>Task 3: Test folder browser with navigation and metadata display</name>
  <files>~/.claude/scripts/folder-browse.sh, folder-info.sh (verification only)</files>
  <action>
    Run comprehensive tests:
    1. Launch folder browser: ~/.claude/scripts/folder-browse.sh
    2. Verify all folders appear in list (15+ entries)
    3. Test search/filtering:
       - Type "claude" → should filter to claude_1769760221 and other claude folders
       - Type "Users" → should filter to -Users-titus- folders
    4. Test preview pane:
       - Navigate with arrow keys
       - Preview updates to show folder metadata
       - All fields readable, formatted nicely
    5. Test selection:
       - Select a folder → returns path
       - Can use returned path for cd or further operations
    6. Test cancellation:
       - Press Escape → returns nothing, closes browser
    7. Test error handling:
       - Navigate to folder with permission issues
       - Navigate to empty folder
       - Verify no crashes, graceful handling

    Verify folder browser works end-to-end with project-select.sh Ctrl-B binding.
  </action>
  <verify>
    All folders visible and searchable
    Preview shows metadata for each folder
    Selection works, returns correct paths
    Cancellation works without errors
  </verify>
  <done>Folder browser fully functional, all metadata displays correctly, navigation smooth</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] folder-browse.sh script created and executable
- [ ] folder-info.sh script created and executable
- [ ] All folders in ~/.claude/projects/ visible in browser (15+)
- [ ] Search/filter working (type to narrow results)
- [ ] Preview pane displays rich metadata for selected folder
- [ ] Selection returns folder path correctly
- [ ] Escape cancels without errors
- [ ] No crashes or permission issues
- [ ] Integration with Ctrl-B from project-select.sh working
</verification>

<success_criteria>

- Folder browser script created and fully functional
- Rich metadata display for all folders
- FZF integration working (search, preview, navigation)
- Can browse and select ANY folder in ~/.claude/projects/
- Ready for Phase 3 Plan 2 (symlink organization)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-browsing-symlinks/03-01-SUMMARY.md`:

# Phase 3 Plan 1: Folder Browser Summary

**Folder browser with recursive navigation and rich metadata display**

## Accomplishments

- `folder-browse.sh` created for recursive folder traversal
- `folder-info.sh` created for rich metadata display
- FZF integration working (search, preview, navigation)
- All 15+ folders visible and searchable
- Preview shows size, file count, sessions, metadata status

## Files Created/Modified

- `~/.claude/scripts/folder-browse.sh` - Folder browser with FZF
- `~/.claude/scripts/folder-info.sh` - Metadata display script

## Decisions Made

None - followed plan as specified.

## Issues Encountered

None

## Next Phase Readiness

Ready for 03-02-PLAN.md: Symlink organization with /active/ and /favorites/ directories
</output>
