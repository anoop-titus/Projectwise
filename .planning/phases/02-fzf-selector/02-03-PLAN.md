---
phase: 02-fzf-selector
type: execute
---

<objective>
Add arrow key navigation polish and integrate browse mode into selector. Ensure all keyboard interactions feel natural and responsive.

Purpose: Complete FZF selector implementation with all modes working seamlessly. Polish keyboard experience.
Output: Refined `project-select.sh` with browse mode and keyboard optimization.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Phase goal from ROADMAP:** FZF-based selection with preview pane, fuzzy search, arrow key navigation.

**Dependency:** Phase 1, Phase 2 Plans 1-2 (preview renderer, multi-mode selector).

**What we're building:**
- Browse mode: shows ALL folders in ~/.claude/projects/ recursively, not just registered projects
- Arrow key optimization: ensure smooth up/down navigation through lists
- Keyboard feedback: visual indication of selection
- Integration: browse mode accessible from quick mode via Ctrl-B binding
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement browse mode for all folders</name>
  <files>~/.claude/scripts/project-select.sh</files>
  <action>
    Create select_browse() function:
    1. Use fd or find to recursively list all directories in ~/.claude/projects/ (depth <= 3)
    2. Exclude: dotfiles, .git, node_modules, .cache
    3. Build FZF input with folder paths
    4. For each folder, attempt to extract metadata:
       - Read PROJECT.json if exists
       - Calculate stats: file count, size, modified date
    5. Display in FZF with optional preview showing folder info
    6. Return selected folder path (whether or not it's in registry)

    This enables exploring/selecting any folder, not just registered projects.
  </action>
  <verify>
    Call: ~/.claude/scripts/project-select.sh browse

    Should show list of all folders, allow selection, return path
  </verify>
  <done>Browse mode implemented, shows all folders, can select non-registered projects</done>
</task>

<task type="auto">
  <name>Task 2: Optimize arrow key navigation and visual feedback</name>
  <files>~/.claude/scripts/project-select.sh</files>
  <action>
    Review FZF configuration across all modes:
    1. Ensure up/down arrows navigate smoothly (standard FZF behavior)
    2. Add visual feedback:
       - Use FZF --color option to highlight selected line
       - Ensure text contrast is readable
       - Use consistent styling across modes
    3. Add --multi if multi-select ever needed (probably not, but option exists)
    4. Ensure Escape always cancels and returns to shell
    5. Test that rapid key presses (holding arrow down) work smoothly

    FZF defaults are good, just ensure consistent configuration.
  </action>
  <verify>
    Test arrow key navigation in each mode:
    - Quick, favorite, category, browse
    - Rapid key presses should feel smooth
    - Selection should be visually obvious
  </verify>
  <done>Navigation smooth, visual feedback clear, keyboard responsiveness good</done>
</task>

<task type="auto">
  <name>Task 3: Test all modes end-to-end and verify keyboard bindings</name>
  <files>~/.claude/scripts/project-select.sh (verification only)</files>
  <action>
    Run comprehensive tests:
    1. Start in quick mode
    2. Press Ctrl-F → should switch to favorite mode
    3. Press Escape → should return to quick mode
    4. Press Ctrl-C → should switch to category mode
    5. Select a category → should show filtered projects
    6. Select a project → should return path
    7. Press Escape to cancel → should return empty, shell prompt available

    Verify all modes work correctly in sequence.
  </action>
  <verify>
    All mode switches work, selections return paths, cancellation works, no errors</verify>
  <done>End-to-end testing passes, all modes integrated and working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Browse mode implemented and shows all folders
- [ ] Arrow key navigation smooth across all modes
- [ ] Visual feedback clear on selection
- [ ] All 4 modes (quick, favorite, category, browse) functional
- [ ] Keyboard bindings (Ctrl-F, Ctrl-C, Ctrl-B) working
- [ ] Escape cancels in all modes
- [ ] End-to-end testing passes
</verification>

<success_criteria>

- All 4 selector modes fully implemented
- FZF navigation smooth and responsive
- Keyboard bindings working seamlessly
- Browse mode enables exploring all folders
- Phase 2 (FZF Selector) complete and ready for Phase 3
  </success_criteria>

<output>
After completion, create `.planning/phases/02-fzf-selector/02-03-SUMMARY.md`:

# Phase 2 Plan 3: FZF Selector Keyboard Bindings Summary

**Browse mode implementation and keyboard navigation polish**

## Accomplishments

- Browse mode implemented for all folders
- Arrow key navigation optimized
- Visual feedback on selection
- All keyboard bindings (Ctrl-F, Ctrl-C, Ctrl-B) integrated

## Files Created/Modified

- `~/.claude/scripts/project-select.sh` - Browse mode and keyboard refinements

## Decisions Made

None - followed plan as specified.

## Issues Encountered

None

## Next Phase Readiness

Phase 2 (FZF Selector) complete. Ready for Phase 3 (Browsing & Symlinks).

All selection modes functional and integrated. Foundation ready for helper commands and .zshrc integration (Phase 4).
</output>
