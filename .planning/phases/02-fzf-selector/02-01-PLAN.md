---
phase: 02-fzf-selector
type: execute
---

<objective>
Build FZF preview renderer that displays project metadata in tabular format. Preview shows: Name | Created | Modified | Category | Status | Git Link.

Purpose: Foundation for all selection modes. Clean, scannable display of project metadata.
Output: `project-preview.sh` script that renders rich metadata for FZF preview pane.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Phase goal from ROADMAP:** FZF-based selection with preview pane, fuzzy search, arrow key navigation.

**Dependency:** Phase 1 (Registry Foundation) - uses `.registry.json` for metadata.

**What we're building:**
- Script that takes project ID as argument
- Reads `.registry.json` and extracts metadata for that project
- Renders using `gum` styling for visual appeal
- Output suitable for FZF preview pane (readable, scannable, not too verbose)
- Handles missing git links gracefully
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project-preview.sh with gum styling</name>
  <files>~/.claude/scripts/project-preview.sh</files>
  <action>
    Create bash script that:
    1. Takes PROJECT_ID as first argument
    2. Reads ~/.claude/projects/.registry.json via jq
    3. Extracts fields: display_name, description, tags[], category, status, created, last_accessed, session_count, git_link
    4. Calculates project folder path
    5. Gets folder stats: file count (find), size (du -sh), modified date (stat)
    6. Renders with gum style:
       - Title with emoji (📦)
       - Description paragraph
       - Metadata rows: Tags, Category, Status, Created, Modified, Sessions, Git Link (if present)
       - Use gum style with border double, padding, colors
    7. Handle missing fields gracefully (show "—" for missing data)
    8. Make script executable
  </action>
  <verify>bash ~/.claude/scripts/project-preview.sh claude_1769760221 produces styled output with all metadata fields</verify>
  <done>Script renders preview correctly, all fields visible, output is scannable and visually organized</done>
</task>

<task type="auto">
  <name>Task 2: Test preview with multiple projects</name>
  <files>~/.claude/scripts/project-preview.sh (verification only)</files>
  <action>
    Test script with at least 3 different projects from registry:
    1. Run: for id in $(jq -r '.projects[].id' ~/.claude/projects/.registry.json | head -3); do echo "Testing $id:"; ~/.claude/scripts/project-preview.sh "$id"; echo; done
    2. Verify each preview renders without errors
    3. Check that missing fields (git_link if not present) show as "—" not errors
    4. Confirm output is readable and not too verbose (fits in typical terminal window)
  </action>
  <verify>Script runs for 3+ different projects without errors, all output readable and formatted consistently</verify>
  <done>Preview works with various projects, handles missing data gracefully, output consistent</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `~/.claude/scripts/project-preview.sh` is executable
- [ ] Script accepts project ID as argument
- [ ] Output uses gum styling (colors, borders, padding visible)
- [ ] All metadata fields displayed (name, description, tags, category, status, dates, git link)
- [ ] Missing fields show "—" not errors
- [ ] Output fits in typical terminal window without truncation
- [ ] Works with multiple different projects from registry
</verification>

<success_criteria>

- Script created and functional
- Preview renders cleanly with gum styling
- All metadata fields visible and readable
- Handles missing data gracefully
- Ready for FZF integration
  </success_criteria>

<output>
After completion, create `.planning/phases/02-fzf-selector/02-01-SUMMARY.md`:

# Phase 2 Plan 1: FZF Selector Preview Summary

**Project metadata preview renderer using gum styling**

## Accomplishments

- `project-preview.sh` created with gum-styled output
- All metadata fields rendered: name, description, tags, category, status, dates, git link
- Handles missing fields gracefully
- Tested with multiple projects from registry

## Files Created/Modified

- `~/.claude/scripts/project-preview.sh` - Preview renderer script

## Decisions Made

None - followed plan as specified.

## Issues Encountered

None

## Next Step

Ready for 02-02-PLAN.md: Multi-mode selector (quick/browse/favorite/category modes)
</output>
