---
phase: 02-fzf-selector
type: execute
---

<objective>
Implement multi-mode project selector using FZF with 4 selection modes: quick (default, recent + fuzzy), browse (ALL folders), favorites (starred only), category (filter by type).

Purpose: Core selection interface. Each mode accessible via keyboard shortcut during FZF interaction.
Output: `project-select.sh` script with 4 modes and FZF configuration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Phase goal from ROADMAP:** FZF-based selection with preview pane, fuzzy search, arrow key navigation.

**Dependency:** Phase 1 (Registry Foundation), Phase 2 Plan 1 (preview renderer).

**What we're building:**
- Main selector script: project-select.sh
- Takes MODE argument: quick (default), browse, favorite, category
- Quick mode: Shows recent projects + allows fuzzy search across all
- Browse mode: All folders recursively with filtering
- Favorite mode: Only starred projects
- Category mode: Two-step: select category, then projects in that category
- FZF configured with: preview pane (via project-preview.sh), arrow key navigation, multi-line display
- Returns selected project folder path
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement quick mode (default FZF selector)</name>
  <files>~/.claude/scripts/project-select.sh</files>
  <action>
    Create select_quick() function:
    1. Read .registry.json and extract all projects
    2. Build FZF input with format: "display_name\tdescription\tcategory\tstatus"
    3. Sort by last_accessed (most recent first)
    4. Launch FZF with:
       - Multi-line input (display name visible)
       - Preview pane running: ~/.claude/scripts/project-preview.sh (pass project ID)
       - Header: "Fuzzy Search - type to find | Ctrl-F: Favorites | Ctrl-C: Categories | Ctrl-B: Browse All"
       - Bind arrow keys for navigation (default FZF behavior)
       - Bind Enter to select
       - Bind Escape to cancel
    5. Extract selected project and return folder path from registry
    6. Handle empty selection (user cancels)

    Make function work as entry point: if MODE not specified, default to quick.
  </action>
  <verify>
    Call: ~/.claude/scripts/project-select.sh quick
    or: ~/.claude/scripts/project-select.sh (defaults to quick)

    Should show FZF with list of projects, preview on right, can type to search
  </verify>
  <done>Quick mode works, FZF displays projects, preview renders, selection returns folder path</done>
</task>

<task type="auto">
  <name>Task 2: Implement favorite and category modes</name>
  <files>~/.claude/scripts/project-select.sh</files>
  <action>
    Create select_favorite() function:
    1. Filter .registry.json for favorite==true only
    2. Show filtered list via FZF with same preview
    3. Return selected project path

    Create select_category() function:
    1. Extract unique categories from .registry.json
    2. Show category list via FZF (simple list, no preview needed)
    3. On selection, filter projects by that category
    4. Show filtered projects via FZF with preview
    5. Return selected project path

    Update main script logic:
    - Check MODE argument
    - Call appropriate function (select_quick, select_favorite, select_category)
    - Return result (folder path or empty string if cancelled)
  </action>
  <verify>
    Call: ~/.claude/scripts/project-select.sh favorite
    Call: ~/.claude/scripts/project-select.sh category

    Favorite mode shows only starred projects
    Category mode shows categories, then projects in selected category
  </verify>
  <done>Favorite and category modes implemented and functional</done>
</task>

<task type="auto">
  <name>Task 3: Set up keyboard bindings in quick mode for mode switching</name>
  <files>~/.claude/scripts/project-select.sh</files>
  <action>
    In select_quick() FZF configuration, add key bindings:
    - Ctrl-F: Execute favorite mode
    - Ctrl-C: Execute category mode
    - Ctrl-B: Execute browse mode (will be implemented in Phase 2 Plan 3)

    Binding format for FZF: --bind='ctrl-f:execute(~/.claude/scripts/project-select.sh favorite)+abort'

    When binding executes (e.g., Ctrl-F pressed in quick mode):
    1. Runs the command (switches to favorite mode)
    2. +abort exits current FZF, new FZF starts for selected mode
    3. User sees seamless mode switch

    Add note in header to inform user of shortcuts
  </action>
  <verify>
    In quick mode, press Ctrl-F → switches to favorite mode
    In quick mode, press Ctrl-C → switches to category mode
    In quick mode, press Ctrl-B → switches to browse mode (once 02-03 complete)
  </verify>
  <done>Keyboard bindings work, mode switching seamless</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `~/.claude/scripts/project-select.sh` is executable
- [ ] Quick mode works: displays projects, allows fuzzy search, preview renders, selection returns path
- [ ] Favorite mode works: shows only starred projects
- [ ] Category mode works: shows categories, then filtered projects
- [ ] Keyboard bindings work: Ctrl-F, Ctrl-C, Ctrl-B switch modes seamlessly
- [ ] All modes handle empty selection (cancel) gracefully
- [ ] FZF displays readable with arrow keys for navigation
</verification>

<success_criteria>

- Main selector script created and functional
- 4 modes implemented and switchable
- FZF preview integration working
- Keyboard bindings enable mode switching
- Returns folder paths for project selection
- Ready for integration into Phase 4 (.zshrc wrapper)
  </success_criteria>

<output>
After completion, create `.planning/phases/02-fzf-selector/02-02-SUMMARY.md`:

# Phase 2 Plan 2: FZF Selector Multi-Mode Summary

**Multi-mode project selector with quick/favorite/category modes and keyboard switching**

## Accomplishments

- `project-select.sh` implemented with 4 modes
- Quick mode: recent projects + fuzzy search
- Favorite mode: starred projects only
- Category mode: two-step category → projects
- Keyboard bindings for seamless mode switching (Ctrl-F, Ctrl-C, Ctrl-B)

## Files Created/Modified

- `~/.claude/scripts/project-select.sh` - Main multi-mode selector script

## Decisions Made

None - followed plan as specified.

## Issues Encountered

None

## Next Step

Ready for 02-03-PLAN.md: Add keyboard bindings refinement and browse mode integration
</output>
